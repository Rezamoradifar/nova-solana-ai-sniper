# Wallet Screenshots

Reserved for real screenshots of the wallet experience: dashboard
`/wallets` and `/wallets/:id` pages, and the Telegram bot's wallet
management screens (balance, backup, restore, transaction history).

None are included yet — see `docs/images/dashboard/README.md` and
`docs/images/telegram/README.md` for why, and how to add them.
