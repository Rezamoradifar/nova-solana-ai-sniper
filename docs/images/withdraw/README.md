# Withdrawal Screenshots

Reserved for real screenshots of the withdrawal experience: the dashboard
`/withdrawals` and `/admin/withdrawals` pages, and the Telegram bot's
`/withdraw`, `/withdrawals`, and `/withdrawstatus` command screens.

None are included yet — see `docs/images/dashboard/README.md` and
`docs/images/telegram/README.md` for why, and how to add them.
